import { KsDndVerticalDirective } from './ks-dnd-vertical.directive';

describe('KsDndVerticalDirective', () => {
  it('should create an instance', () => {
    const directive = new KsDndVerticalDirective();
    expect(directive).toBeTruthy();
  });
});
