import { KsDndHorizontalDirective } from './ks-dnd-horizontal.directive';

describe('KsDndHorizontalDirective', () => {
  it('should create an instance', () => {
    const directive = new KsDndHorizontalDirective();
    expect(directive).toBeTruthy();
  });
});
