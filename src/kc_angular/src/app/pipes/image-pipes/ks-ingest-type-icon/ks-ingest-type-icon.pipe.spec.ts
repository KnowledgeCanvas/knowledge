import { KsIngestTypeIconPipe } from './ks-ingest-type-icon.pipe';

describe('KsIngestTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new KsIngestTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
