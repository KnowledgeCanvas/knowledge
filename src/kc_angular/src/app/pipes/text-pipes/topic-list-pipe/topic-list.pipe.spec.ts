import { TopicListPipe } from './topic-list.pipe';

describe('TopicListPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicListPipe();
    expect(pipe).toBeTruthy();
  });
});
