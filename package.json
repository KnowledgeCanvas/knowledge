{
  "name": "knowledge-canvas",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "workspaces": [
    "kc_electron",
    "kc_workspace"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RobRoyce/knowledge-canvas.git"
  },
  "main": "kc_electron/dist/electron.js",
  "scripts": {
    "build": "yarn workspaces run build",
    "build-dev": "yarn workspaces run build-dev",
    "clean": "rm -rf ./dist && yarn workspaces run clean",
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "purge": "rm -rf node_modules && yarn workspaces run purge",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-main": "cd kc_workspace && yarn run watch-main",
    "watch-main-dev": "cd kc_workspace && yarn run watch-main-dev",
    "watch-electron": "cd kc_electron && yarn run watch-electron",
    "watch-electron-dev": "cd kc_electron && yarn run watch-electron-dev"
  },
  "dependencies": {},
  "devDependencies": {},
  "build": {
    "appId": "com.knowledgecanvas.app",
    "productName": "Knowledge Canvas Application",
    "directories": {
      "buildResources": "resources"
    },
    "files": [
      "kc_workspace/dist/**/*",
      "kc_electron/dist/**/*",
      "resources/**/*"
    ],
    "extraResources": [
      {
        "from": "resources/app.env",
        "to": "app.env"
      }
    ],
    "mac": {
      "category": "knowledge.canvas.app"
    },
    "win": {
      "target": [
        "portable"
      ]
    }
  }
}
